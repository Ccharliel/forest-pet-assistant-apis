CREATE DATABASE IF NOT EXISTS pospal CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 后续若需要其他数据库，在此追加即可，例如：
-- CREATE DATABASE IF NOT EXISTS agent_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;